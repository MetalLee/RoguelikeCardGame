## 目的

策划一款 Roguelike 卡牌独立游戏。本仓库作为游戏知识库，用于维护游戏相关的设计文档和资料，帮助开发者随时记录想法，并将碎片化资料有条理地整理到此知识库中。

## 目录结构

本知识库使用 Obsidian 知识库管理：

- `inspiration/`：用于开发者随手记录玩法灵感，或记录每次用户与 Codex 的问答交互。
- `insight/`：同类游戏设计与评价洞察。
- `design/`：按照业界标准的游戏设计维度划分子目录，将灵感与洞察整理到对应文档中。
- `game/`：Godot 游戏项目目录，Godot 工程根目录为 `game/project.godot`。
- `AGENTS.md`：本文件，记录仓库工作规则与当前开发任务队列。

## Obsidian 兼容规则

本仓库不强制依赖开发者安装 Obsidian；Codex 可以直接在 Markdown 知识库内闭环整理、维护和查询文档。但所有由开发者或 Codex 落地的文档，必须保持 Obsidian 知识库友好：

- 使用 Markdown 文件作为知识载体。
- 需要关联其他文档时，优先使用 Obsidian 双链语法，如 `[[design/01_core_gameplay/03_card_system|卡牌系统]]`。
- 文件路径、文档标题、交叉链接应保持稳定，避免随意重命名导致双链失效。
- 新增文档应放入符合语义的目录；若现有目录无法覆盖，再创建新文档或新子目录。
- 文档内容应方便在 Obsidian 中通过反向链接、图谱、标签和搜索进行管理。

## 整理原则

当 `inspiration/` 有文件新建或修改时：

1. 提炼开发者灵感核心想法、观点。
2. 匹配 `design/` 中相关文档，如有冲突或模糊观点可以追问开发者至明确。
3. 增加对应的交叉链接，方便 Obsidian 管理。
4. 如果无对应文档，则创建新文档。

当 `insight/` 有文件新建或修改时：

1. 核对资讯来源，分析可靠性，提炼与本项目有关的关键内容。
2. 匹配 `design/` 中相关文档，分析可参考点、可改进点。
3. 增加对应的交叉链接，方便 Obsidian 管理。
4. 如果无对应文档，则创建新文档。

## 查询原则

游戏开发请优先遵循 `design/` 中的所有设计与规范，仅必要时查看 `inspiration/`、`insight/`。

## 新版《剑与黑塔》MVP 实现基线

当前 MVP 已从旧连锁 / 技能牌 / 卡牌包原型，迁移为《剑与黑塔》的新版世界观与核心玩法：

- 游戏名与世界观按《剑与黑塔》落地：核后失色世界、黑塔、勇者联盟、左与茵为正式设定基线。
- 正式卡牌类型只保留行动牌与终结牌，不再新增技能牌逻辑。
- 战斗资源以 6 格彩能池为核心；旧 3 / 5 / 8 阈值不再作为新版主流程规则。
- 行动牌主要生成彩能；终结牌按自身标注消耗彩能，并按实际消耗颜色结算追加效果。
- 五色为红、黄、蓝、绿、紫；黄色只表示增加卡牌释放次数，不表示抽牌、返还行动点或回能量。
- 武器是角色机制和卡池入口，不是传统装备掉落；当前 MVP 只支持左轮剑与机械臂。
- 开局流程为自由选择主手 / 副手武器；每把武器固定 6 张初始牌（4 行动 + 2 终结），系统自动将主手 6 张全部加入、将副手 4 张行动牌加入，组成 10 张初始 `CardInstance`，不再提供起始牌自由选择。
- 普通战斗奖励已替换为随机色彩碎片 + 武器卡三选一；色彩碎片用于给未附魔行动牌附魔。
- 玩家可见文本统一使用“魔物”；C# 内部 `Enemy` 命名可暂时保留。
- 正式 gameplay 数据已合并到 `game/data/gameplay/`；代码、校验器和调试工具不再读取 `game/data/gameplay_v2/`。
- `game/data/gameplay/encounters/*.json` 中 `card_pack_ids` 仅作为空兼容字段保留；非空旧卡牌包引用应被校验器拒绝。

## 当前硬约束

- Godot 工程根目录是 `game/`。
- 游戏实现文件放在 `game/` 下；知识库文档仍放在 `design/`、`inspiration/`、`insight/`。
- 玩法规则层与 Godot 表现层分离；C# 规则逻辑不得依赖 Godot 节点树。
- 卡牌、魔物、遗物、遭遇、色彩、武器、卡池和奖励优先数据驱动，不要把内容数值写死在场景里。
- 不引入外部依赖，除非开发者明确批准并同步记录原因。
- 不创建二进制文件，除非任务明确要求生成或导入美术 / 音频资源。
- 不恢复 `skill`、旧 `chain`、`min_chain`、3 / 5 / 8 阈值、旧行动 / 技能 / 终结卡牌包作为新版主流程。
- 新增或修改代码、数据时，同步补充可运行校验、测试或最小验证方式。
- 如果改动影响设计理解、制作范围或长期维护成本，同步更新对应 `design/` 文档、[[design/08_governance/01_change_log|变更日志]] 或 [[design/08_governance/00_decision_log|决策记录]]。

## 后续开发任务队列

以下 task 是当前 MVP 在大修改为《剑与黑塔》设定和玩法后，建议继续推进的工作顺序。每次进入下一 task 前，应先保证当前 task 的数据校验、规则测试、主工程构建和可用 Godot headless 检查稳定通过。

### Task 1：完成新版数据入口收敛与兼容层审计

状态：已完成（2026-06-14）

目标：确认正式运行时只使用 `game/data/gameplay/` 的新版数据，旧原型路径不会被误用。

工作范围：
- 全仓库确认 `gameplay_v2` 不再出现在 `game/` 代码、数据、工具、测试和 README 中。
- 审计 `card_pack_ids`、`starter_deck` 等兼容字段，保持字段可读但不驱动新版主流程。
- 清理或隔离旧 `reward_pack`、旧 skill 卡、旧 chain 原型数据和旧表现层引用。
- 明确哪些旧素材文件只是历史资源，不进入 active data manifest。

验证：
- `.\game\tools\data_validator\validate_data.ps1`
- `dotnet run --project game\tests\Unit\RoguelikeCardGame.Tests.csproj`
- `dotnet build game\RoguelikeCardGame.csproj`
- `.\game\tools\check_project.ps1`

完成结果：
- `gameplay_v2` 已不出现在 `game/` 代码、数据、工具、测试和 README 中；仅保留在本文件的历史规则描述里。
- `card_pack_ids` 与 `starter_deck` 继续作为空兼容字段可读；正式开局、调试默认牌组和奖励预览不再由旧固定牌组或旧卡牌包驱动。
- 规则层移除旧 `Chain` / `MinChain` / `DefaultChainChange` 兼容模型，删除未使用的旧 `RewardPackDefinition`。
- `game/data/presentation/assets.json` 不再登记旧 chain / skill / reward pack 资源；历史 PNG 与 `.import` 文件可留在磁盘上，但不进入 active data manifest。
- 数据校验器已补充 active presentation 旧资源 token 检查，非空旧卡牌包奖励继续被拒绝。
- 已验证通过：数据校验、规则单测、主工程构建、Godot .NET headless 项目检查。

### Task 2：补齐新版内容加载与运行时数据映射

状态：已完成（2026-06-14）

目标：让运行时内容加载器完整覆盖新版 weapons / colors / cards / card_pools / encounters / localization。

工作范围：
- 检查 `GameContent` 对武器、色彩、卡牌、卡池、魔物、遭遇、遗物、本地化和表现数据的映射是否完整。
- 为缺失字段补充解析和默认值，避免 UI 或规则层靠字符串猜测。
- 检查 `CardDefinition`、`WeaponDefinition`、`ColorDefinition`、`CardPoolDefinition` 与 JSON Schema 的字段一致性。
- 为内容加载失败提供清晰错误信息。

验证：
- 数据校验器通过。
- 单元测试覆盖至少 1 个完整内容加载样例。
- Godot headless 能启动主场景。

完成结果：
- `GameContent` 已补齐 `colors` 入口，并通过 `ColorsById` 暴露五色角色、基础效果模板和叠加规则。
- `GameContent` 改为使用正式 `WeaponDefinition`，卡池模型收敛为 `CardPoolDefinition`，覆盖 weapons / card_pools schema 中的起始池、奖励池和标签字段。
- `CardDefinition` 保留现有规则层派生字段，同时补充 `costs`、`requirements`、`targeting`、`color_interactions`、`after_play`、`balance`、`color_filter`、`fixed_color_id` 等新版 JSON 结构化字段，避免 UI 和工具靠字符串猜测。
- 新增 `GameContent.LoadFromDataRoot()`，Godot 运行时仍可使用 `LoadFromProject()`；内容加载失败会报告文件路径、条目索引和内容 ID。
- 单元测试已纳入 `Infrastructure/Content`，并用真实 `game/data` 覆盖 1 个完整内容加载样例，检查颜色、武器、卡牌、卡池、遭遇、本地化和表现资源映射。
- 已验证通过：数据校验、规则单测、主工程构建、Godot .NET headless 项目检查。

### Task 3：高优先级替换占位 UI 与旧 UI 资产

目标：尽快用符合《剑与黑塔》新版视觉方向的 UI 资产替换当前占位 UI 和旧连锁 / 旧卡牌包 UI，降低试玩时的视觉误导。

优先级说明：
- 本 task 优先级高于进一步数值调参、复杂规则扩展和事件系统。
- 只要某个界面仍明显使用旧连锁、旧技能牌、旧卡牌包或纯工程占位控件，就应先处理 active UI 表达。
- 可先使用稳定、统一、可读的临时美术资源，不要求一次达到最终商业美术质量。

工作范围：
- 清点 active UI 中仍在使用的旧连锁、旧阈值、技能牌、卡牌包、旧 reward pack、旧色调或纯占位资源。
- 替换主菜单、武器选择、自动起始牌组说明、战斗 HUD、手牌、卡牌 tooltip、奖励界面、结算界面的核心 UI 资产。
- 战斗 HUD 使用 6 格彩能槽，清晰显示无色 / 红 / 黄 / 蓝 / 绿 / 紫构成。
- 行动牌卡面显示行动点、武器归属、附魔颜色和生成彩能；终结牌卡面显示彩能需求和消耗方式。
- 奖励界面改为色彩碎片 + 武器卡三选一表达，不再出现旧行动 / 技能 / 终结卡牌包视觉。
- 更新 `game/data/presentation/` 的 asset manifest，只登记 active UI 需要的资源；历史素材可留在磁盘，但不得被 active UI 引用。
- 若暂时缺少最终美术，用统一漫画线稿 / 纸张 / 粗线框风格占位，避免混用旧暗色或旧机制视觉。

验证：
- `.\game\tools\data_validator\validate_data.ps1`
- `dotnet build game\RoguelikeCardGame.csproj`
- Godot headless 项目检查。
- 人工检查 active UI 不再引用旧连锁阈值、技能牌模板、旧卡牌包表现作为核心反馈。

### Task 4：建立关键视觉母版与 UI 资产规范

目标：为后续所有 UI 资产替换建立统一视觉母版，避免继续生成彼此不一致的图片。

工作范围：
- 先清理设计文档中不符合当前方向的旧“暗黑哥特”视觉描述。
- 生成或归档一张关键视觉母版，包含简洁漫画剑客主角、简洁漫画魔物、行动牌 / 终结牌示意、色彩碎片、武器卡奖励、遗物和黑塔世界观线索。
- 当前正式卡牌类型不再包含技能牌；如视觉历史参考中出现技能牌，应标记为废弃原型，不进入新版 UI。
- 明确卡牌框、彩能槽、按钮、面板、奖励选择、结算页、魔物状态条的视觉规则。
- 将可复用 UI 资产的命名、尺寸、安全区和 `asset_id` 规则写入 `design/03_experience/00_ui_ux.md` 或相关视觉方向文档。

验证：
- 视觉方向文档更新。
- 新资产引用通过数据校验。
- Godot headless 能加载资源 manifest。

### Task 5：打磨彩能 / 色彩结算规则边界

目标：把五色 MVP 规则从“可用”打磨到“可解释、可调参、可防爆”。

工作范围：
- 审计 `CardPlayService` 中行动牌生成彩能、行动牌附魔增强、终结牌消耗彩能与逐色追加结算。
- 给黄色额外释放、紫色复制 / 放大、绿色治疗、蓝色防御、红色增伤补充硬上限与结构化日志。
- 检查终结牌消耗固定数量、X、全部彩能时的颜色选择与预览一致性。
- 确认绿色回复不超过最大生命，蓝色防御遵守现有防御清空规则。

验证：
- 每种颜色至少 1 个规则测试。
- 黄色不触发抽牌、返还行动点或回能量测试。
- 紫色不会无限循环测试。

### Task 6：完善战斗预览与不可打出原因

目标：让规则层提供 UI 可直接展示的行动牌 / 终结牌预览结果。

工作范围：
- 行动牌预览显示基础效果、附魔颜色、生成彩能、颜色追加效果、最终估算值。
- 终结牌预览显示消耗数量、消耗颜色构成、基础效果、逐色追加、最终预估结果。
- 不可打出原因收敛为行动点不足、彩能不足、目标缺失、非玩家回合、手牌实例不存在等新版原因。
- 移除玩家可见的连锁不足、技能牌、阈值表达。

验证：
- 规则层预览测试覆盖行动牌、单色终结牌、多色终结牌。
- UI smoke 测试或 Godot headless 启动不报错。

### Task 7：重检战斗 UI 的新版信息表达

目标：确保战斗界面只表达彩能 / 色彩 / 武器卡牌结构，不再露出旧连锁 UI。

工作范围：
- HUD 使用 6 格彩能槽，显示无色 / 红 / 黄 / 蓝 / 绿 / 紫构成。
- 手牌卡面显示行动点、武器归属、附魔颜色、生成彩能或终结牌彩能需求。
- Tooltip 显示行动牌附魔追加与终结牌逐色结算拆解。
- 战斗日志补充彩能获得、彩能消耗、黄色额外释放、紫色放大提示。
- 移除 active UI 对 `chain_meter_8_slots`、`chain_threshold_*`、技能牌模板、旧卡牌包表现的依赖。

验证：
- `dotnet build game\RoguelikeCardGame.csproj`
- `dotnet run --project game\tests\Unit\RoguelikeCardGame.Tests.csproj`
- Godot headless 项目检查。

### Task 8：打磨开局主副武器与自动起始牌组

目标：让玩家明确理解左轮剑 / 机械臂的角色定位，并在选择主副武器后稳定进入系统自动组成的 10 张初始牌组。

工作范围：
- 武器选择界面展示主手 / 副手选择关系、武器说明和当前可选状态。
- 武器选择界面展示主手 6 张全带、副手 4 张行动牌带入的自动组牌规则，以及两把武器的固定初始池摘要。
- 起始牌自由选择界面不进入当前 MVP 主流程；可保留卡池与校验实现作为未来拓展，但运行时视为系统自动帮玩家选好 10 张卡。
- 校验固定起始池必须为 6 张，且为 4 行动 + 2 终结；禁止池外卡、数量错误、同一实例重复异常。
- 确认固定 `starter_deck` 不再驱动新版开局，只作为空兼容字段存在。

验证：
- 左轮剑主手 + 机械臂副手测试。
- 机械臂主手 + 左轮剑副手测试。
- 自动组牌结果为 10 张测试；少选 / 多选 / 池外卡仍由保留校验拒绝。

### Task 9：打磨战后色彩碎片 + 武器卡奖励

目标：让战后奖励形成新版构筑核心，而不是旧卡牌包残影。

工作范围：
- 奖励第一步展示随机色彩碎片颜色和规则含义。
- 第二步只允许选择未附魔行动牌，已附魔行动牌和终结牌不可选。
- 第三步从主手 / 副手武器奖励池生成三选一，选择 1 张作为新 `CardInstance` 加入牌组。
- 同名卡允许作为新实例加入牌组。
- 精英保留固定遗物奖励；Boss 保留 MVP 通关结算或简化 Boss 奖励展示。

验证：
- 随机色彩碎片生成测试。
- 只能附魔未附魔行动牌测试。
- 武器卡三选一只能选 1 张测试。
- 奖励后进入下一场战斗测试。

### Task 10：完善完整 MVP Run 回归闭环

目标：从开局、战斗、奖励、下一战、Boss、通关 / 失败形成稳定闭环。

工作范围：
- 主菜单开始后进入武器选择，并自动创建初始卡组后进入战斗。
- 战斗胜利进入奖励；奖励确认后进入下一场。
- 精英胜利发放遗物并继续流程。
- Boss 胜利进入通关结算。
- 玩家生命归零进入失败结算。
- 重开时清理旧 Run / Combat / Reward 状态。

验证：
- 完整 MVP Run 可从开局选武器进入战斗。
- 战后能附魔并拿 1 张武器卡。
- 能进入下一场战斗。
- Boss 胜利进入通关结算。
- 玩家生命归零进入失败结算。

### Task 11：补齐 Debug / Metrics 新版指标

目标：让试玩调参能看到彩能、附魔、武器路线和终结牌表现。

工作范围：
- 移除最高连锁、阈值达成等旧指标。
- 统计彩能峰值、颜色构成、终结牌颜色消费、附魔使用率。
- 统计三条早期构筑路线信号：蓝绿重炮流、红色机械防反流、黄紫弹幕调色流。
- 调试脚本从武器固定起始池或显式调试配置生成默认调试牌组，不读取旧奖励包文件。
- 导出日志能标记色彩碎片、附魔目标、武器卡奖励选择和新实例 ID。

验证：
- debug session 脚本可生成日志和 metrics。
- 单元测试覆盖关键 metrics 聚合。

### Task 12：内容数值与卡牌文本第一轮试玩调参

目标：让 20 张左轮剑 / 机械臂卡牌和 6 场 MVP 遭遇达到可试玩节奏。

工作范围：
- 检查 20 张卡是否都有卡名、规则文本、简短风味文本。
- 检查行动牌 / 终结牌比例、费用、伤害、防御、治疗、控制、彩能生成和消耗。
- 检查每种武器固定 6 张起始池是否满足 4 行动 + 2 终结结构，并具备足够基础攻击、防御、彩能生成和终结牌入口。
- 检查普通、精英、Boss 魔物生命、意图和伤害节奏。
- 保持玩家可见文本使用“魔物”，不使用“恶魔”。

验证：
- 数据校验器通过。
- 至少跑通 3 次人工或调试 MVP 流程，记录卡手、过强、过弱和解释不清的问题。

### Task 13：事件系统最小切片

目标：在战斗系统稳定后，按当前路线优先制作事件，而不是商店或路线图。

工作范围：
- 建立事件数据 Schema 与最小运行时模型。
- 事件以碎片化叙事为主，表达黑塔谜团、左与茵、勇者联盟和核后失色世界。
- 事件奖励可先限制为生命变化、获得色彩碎片、获得指定武器卡、获得遗物或跳过。
- 不做复杂地图、商店、休息点或长期成长。

验证：
- 事件数据校验通过。
- Run 流程能在两场战斗之间插入一个固定事件并继续。

### Task 14：保存 / 读取 Run 状态

目标：为后续试玩、复盘和崩溃恢复建立最小存档能力。

工作范围：
- 序列化 `RunState` 中的 seed、主副武器、MasterDeck 实例、附魔状态、持有遗物、当前节点和生命状态。
- 明确哪些战斗中状态不保存，哪些 Run 级状态必须保存。
- 避免保存 Godot 节点或表现层对象。
- 增加版本号与迁移占位，方便后续数据结构变化。

验证：
- RunState 序列化 / 反序列化测试。
- 读取后能进入下一场战斗或结算页。

### Task 15：发布前 MVP 稳定性清单

目标：在进入公开试玩或录屏前，统一做一次工程稳定性收口。

工作范围：
- 清理明显未使用的旧数据入口、旧 UI 组件和旧文档误导表述。
- 确认 Godot 主场景、菜单、战斗、奖励、结算、重开流程都可重复进入。
- 检查异常路径：无可附魔行动牌、奖励池不足 3 张、卡组为空、魔物数据缺失、文本键缺失。
- 确认数据校验器覆盖新增数据文件。
- 更新 README 和变更日志。

验证：
- `.\game\tools\data_validator\validate_data.ps1`
- `dotnet run --project game\tests\Unit\RoguelikeCardGame.Tests.csproj`
- `dotnet build game\RoguelikeCardGame.csproj`
- Godot headless 项目检查。
